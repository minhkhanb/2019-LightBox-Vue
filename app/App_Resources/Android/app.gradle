// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "success.app.login"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
